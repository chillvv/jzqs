# 收货地址重置操作手册

> 目的：停用所有历史收货地址，强制客户周一通过微信地图选点重新录入，让地址都带上坐标（`latitude`/`longitude`），骑手端才能精准导航。
> 执行时间：**周六晚 24:00 后（周日凌晨）**，周日不营业。
> ⚠️ 本手册为 **软删除** 版本（V35 起地址删除即停用），**严禁物理 DELETE 地址**（V36 起数据库会用外键直接拒绝，见第八节）。

---

## 一、为什么需要这次重置

骑手端导航依赖订单关联地址的坐标：

- 有坐标 → `wx.openLocation` 精准导航
- 无坐标 → 复制地址 + 提示骑手自行搜索

历史地址是导入/早期手工录入的，没有经过微信地图选点，`customer_addresses.latitude / longitude` 全部为 `NULL`（V32 迁移才补的字段），导致骑手端"点导航"始终退化为"复制地址"。

---

## 二、执行前提（必须全部满足）

1. 周日不营业，无待配送 / 进行中的订单
2. 周六的订单已全部送达（或已处理完）
3. 已备份数据库
4. **V36 外键迁移已上线**（`V36__address_reference_foreign_keys.sql`），否则物理误删无法被数据库拦截

---

## 三、执行前：备份

```sql
-- 备份将被影响的关键表（YYYYMMDD 换成当天日期，如 20260906）
CREATE TABLE customer_addresses_bak_20260906     AS SELECT * FROM customer_addresses;
CREATE TABLE subscription_rules_bak_20260906     AS SELECT * FROM subscription_rules;
CREATE TABLE rider_address_bindings_bak_20260906 AS SELECT * FROM rider_address_bindings;
CREATE TABLE address_reference_images_bak_20260906 AS SELECT * FROM address_reference_images;
```

> 注意：`address_reference_images`（门牌参考图）会被清空且不可由软删除恢复，**必须备份**。

---

## 四、执行 SQL（按顺序，一个事务）

```sql
START TRANSACTION;

-- 1. 全部地址停用（软删除）：保留地址行，历史订单的 address_id 永不悬空，
--    骑手端/派单中心/顾客端 INNER JOIN 照常命中，订单零影响。
UPDATE customer_addresses SET active = 0, is_default = 0;

-- 2. 订阅规则：默认地址置空（订阅自动下单会回退到"先加地址"分支，
--    避免生成空地址订单）。
UPDATE subscription_rules SET default_address_id = NULL;

-- 3. 骑手派单记忆（区域归属）：全清（重新录入后地址 id 全部更换，旧记忆作废）。
DELETE FROM rider_address_bindings;

-- 4. 地址参考图：全清。
DELETE FROM address_reference_images;

COMMIT;
```

> 若订阅确认表有未确认记录，一并处理：
> `UPDATE subscription_confirmations SET address_id = NULL WHERE address_id IS NOT NULL;`

---

## 五、执行后验证

```sql
-- 1) 有效地址应为 0
SELECT COUNT(*) FROM customer_addresses WHERE active = 1;

-- 2) 地址总行数应 > 0（墓碑行保留，这是正常的）
SELECT COUNT(*) FROM customer_addresses;

-- 3) 订单孤儿引用必须为 0（三端数量一致性的硬校验）
SELECT COUNT(*) FROM meal_slot_orders mso
  LEFT JOIN customer_addresses ca ON ca.id = mso.address_id
  WHERE ca.id IS NULL;

-- 4) 订阅默认地址应全部为空
SELECT COUNT(*) FROM subscription_rules WHERE default_address_id IS NOT NULL;

-- 5) 骑手记忆 / 参考图应为 0
SELECT COUNT(*) FROM rider_address_bindings;
SELECT COUNT(*) FROM address_reference_images;
```

---

## 六、影响与注意事项

1. **订单完全不受影响**（与物理 DELETE 的本质区别）：
   - 历史订单 `meal_slot_orders.address_id` 仍指向**存在的地址行**（只是 active=0），
     订单查询 JOIN 时不按 active 过滤，骑手端 / 派单中心 / 顾客端"我的订单"照常显示，
     地址文本也照常显示。**顾客不会看到订单消失**。
   - 钱包流水 `wallet_transactions` 只挂 `related_order_id`，不依赖地址表，永远可查。
2. **订阅用户周一需重新选默认地址**：订阅设置会要求先有可用地址，否则报
   "您还未添加收货地址，请先在个人中心添加"；后台"导入订阅订单"预览的 address_id 为空，
   导入前必须先让订阅用户重新录入。
3. **顾客下单同样会提示先加地址**（下单前必须有 active=1 的地址），这正是强制重录的手段。
4. **后台编辑订单 / 手动建单**在地址清空期间会因无可用地址而失败，属于预期。
5. **地址名额不受墓碑影响**：地址上限按 `active = 1` 统计，停用地址不占用名额。
6. **内存/磁盘开销可忽略**：一行地址约 100~200 字节；所有业务查询都带 `active = 1` 过滤，
   墓碑行不会被读进内存。回收方式见第七节。

---

## 七、墓碑回收（可选，日常运维）

只有**零引用**的停用地址才能物理删除。推荐挂在每天凌晨的清理任务里跑，且必须先跑"订单历史清理"：

```sql
DELETE ca FROM customer_addresses ca
WHERE ca.active = 0
  AND NOT EXISTS (SELECT 1 FROM meal_slot_orders         mso WHERE mso.address_id = ca.id)
  AND NOT EXISTS (SELECT 1 FROM subscription_confirmations sc  WHERE sc.address_id  = ca.id)
  AND NOT EXISTS (SELECT 1 FROM subscription_rules       sr  WHERE sr.default_address_id = ca.id)
  AND NOT EXISTS (SELECT 1 FROM address_reference_images ari WHERE ari.customer_address_id = ca.id)
  AND NOT EXISTS (SELECT 1 FROM rider_address_bindings   rb  WHERE rb.address_id  = ca.id);
```

> 顺序很重要：**先清订单，再清地址**。订单还在保留期内时这条 SQL 删不掉任何行，
> 这正是外键保护下的正确行为。

---

## 八、V36 外键护栏（已随迁移上线）

`V36__address_reference_foreign_keys.sql` 为地址引用补齐外键，物理误删会被数据库直接拒绝：

| 引用 | 策略 | 含义 |
|---|---|---|
| `meal_slot_orders.address_id` | RESTRICT | 有订单引用的地址禁止物理删除 |
| `subscription_confirmations.address_id` | RESTRICT | 有确认单引用则禁止删除 |
| `subscription_rules.default_address_id` | SET NULL | 地址真被删则自动回退重新选地址 |
| `rider_address_bindings.address_id` | CASCADE | 区域记忆随地址一起清 |
| `address_reference_images.customer_address_id` | CASCADE | 门牌图随地址一起清 |

---

## 九、回滚（如有意外）

```sql
-- 1) 恢复地址：按 id 回写软删状态
UPDATE customer_addresses ca
JOIN customer_addresses_bak_20260906 b ON b.id = ca.id
SET ca.active = b.active, ca.is_default = b.is_default;

-- 2) 恢复订阅默认地址
UPDATE subscription_rules sr
JOIN subscription_rules_bak_20260906 b ON b.id = sr.id
SET sr.default_address_id = b.default_address_id;

-- 3) 恢复骑手区域记忆（先清空再回灌，避免主键冲突）
TRUNCATE TABLE rider_address_bindings;
INSERT INTO rider_address_bindings SELECT * FROM rider_address_bindings_bak_20260906;
```

> 注意：执行后新增的地址/订单不会回滚；`address_reference_images` 只能从备份表回灌。

---

## 十、前置依赖确认（执行前确认均已上线）

1. 用户端「新增 / 编辑地址」走微信地图选点（`wx.chooseLocation`），坐标随地址落库（V32 已加 `latitude`/`longitude` 字段）✅
2. 骑手端订单详情接口已返回 `latitude`/`longitude`，有坐标时走 `wx.openLocation` 精准导航 ✅
3. V36 地址引用外键已上线（`V36__address_reference_foreign_keys.sql`）
