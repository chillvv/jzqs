# 收货地址重置操作手册

> 目的：清空所有历史收货地址，强制客户周一通过微信地图选点重新录入，让地址都带上坐标（`latitude`/`longitude`），骑手端才能精准导航。
> 执行时间：**周六晚 24:00 后（周日凌晨）**，周日不营业。

---

## 一、为什么需要这次重置

骑手端导航依赖订单关联地址的坐标：

- 有坐标 → `wx.openLocation` 精准导航
- 无坐标 → 复制地址 + 提示骑手自行搜索

历史地址是导入/早期手工录入的，没有经过微信地图选点，`customer_addresses.latitude / longitude` 全部为 `NULL`（V32 迁移才补的字段），导致骑手端"点导航"始终退化为"复制地址"。

---

## 二、执行前提（必须全部满足）

1. 周日不营业，无待配送 / 进行中的订单
2. 周六的订单已全部送达（或可接受历史订单地址变空）
3. 已备份数据库

---

## 三、执行前：备份

```sql
-- 备份将被影响的关键表（YYYYMMDD 换成当天日期，如 20260906）
CREATE TABLE customer_addresses_bak_20260906     AS SELECT * FROM customer_addresses;
CREATE TABLE subscription_rules_bak_20260906     AS SELECT * FROM subscription_rules;
CREATE TABLE rider_address_bindings_bak_20260906 AS SELECT * FROM rider_address_bindings;
```

---

## 四、执行 SQL（按顺序，一个事务）

```sql
START TRANSACTION;

-- 1. 订阅规则：默认地址置空（避免周一订阅用户自动下单生成空地址订单）
UPDATE subscription_rules SET default_address_id = NULL;

-- 2. 骑手派单记忆（区域归属）：全清（地址 id 全部重排，旧记忆作废）
DELETE FROM rider_address_bindings;

-- 3. 地址参考图：全清
DELETE FROM address_reference_images;

-- 4. 最后清空所有收货地址
DELETE FROM customer_addresses;

COMMIT;
```

---

## 五、执行后验证

```sql
-- 都应为 0
SELECT COUNT(*) FROM customer_addresses;
SELECT COUNT(*) FROM subscription_rules WHERE default_address_id IS NOT NULL;
SELECT COUNT(*) FROM rider_address_bindings;
```

---

## 六、影响与注意事项

1. **历史订单配送地址变空**：`meal_slot_orders.address_id` 指向已删除的地址，骑手端查历史订单时地址显示为空（订单已完成，影响很小）。
2. **订阅用户周一需重新选默认地址**：`default_address_id` 已置空，订阅用户周一进入订阅设置需重新选择收货地址；否则订阅自动下单会生成空地址订单。
3. **周一客户重新录入地址**：客户下单 / 添加地址时，前端走微信地图选点，录入带坐标的地址。

---

## 七、回滚（如有意外）

```sql
INSERT INTO customer_addresses     SELECT * FROM customer_addresses_bak_20260906;
INSERT INTO subscription_rules     SELECT * FROM subscription_rules_bak_20260906;
INSERT INTO rider_address_bindings SELECT * FROM rider_address_bindings_bak_20260906;
```

> 注意：回滚前先 `TRUNCATE` 原表，避免主键冲突；且期间新增的订单地址不会回滚。

---

## 八、前置依赖确认（执行前确认均已上线）

1. 用户端「新增 / 编辑地址」走微信地图选点（`wx.chooseLocation`），坐标随地址落库（V32 已加 `latitude`/`longitude` 字段）
2. 骑手端订单详情接口已返回 `latitude`/`longitude`，有坐标时走 `wx.openLocation` 精准导航
