spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/jzqs_test_${random.uuid}?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root
app:
  auth:
    filter:
      enabled: false
  jwt:
    allow-default-secret: true
